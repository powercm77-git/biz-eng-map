# CLAUDE.md — biz-eng-map 작업 인계 문서

새 세션(대화)을 열 때 이 파일을 먼저 읽는다. 사용자(송정호 박사님)는 기억이 짧아,
이전 대화 내용을 다시 설명하지 않아도 여기서 상태를 파악해야 한다.

> ## ★ 새 세션이 가장 먼저 할 일
>
> 1. **배포 상태부터 확인**: `git status`(로컬-원격 동기화 여부), 라이브 사이트
>    https://powercm77-git.github.io/biz-eng-map/ 가 실제로 뜨는지, 최신 커밋과
>    Pages 빌드가 일치하는지(`gh api repos/powercm77-git/biz-eng-map/pages/builds/latest`).
> 2. **도서관 좌석 워크플로가 살아있는지 확인**: `gh run list --repo powercm77-git/biz-eng-map
>    --workflow=update-library-data.yml --limit 3` — 최근 실행이 `success`인지. 이 워크플로는
>    과거 5일간 조용히 실패한 전적이 있다(2-2절). **실패 중이어도 사이트 자체는 멀쩡히 뜨니
>    눈치채기 어렵다 — 반드시 직접 확인할 것.**
> 3. **`.github/workflows/*.yml`을 고쳐야 하면 2-1절부터 읽을 것.** 이 저장소 특유의 인증
>    문제(OAuth App workflow scope)가 있다 — 시간 낭비하지 않으려면 먼저 읽어야 한다.
> 4. 이 프로젝트는 **넷리파이(Netlify)를 쓰지 않는다.** GitHub Pages다(`main` 브랜치 루트,
>    빌드 단계 없이 `index.html`을 그대로 서빙). 혼동해서 넷리파이 관련 설정을 찾지 말 것.
> 5. **★★ 이 저장소는 절대 비공개로 전환하지 말 것(2026-08-18 실제 장애로 확인).**
>    Free 요금제에서는 저장소를 private으로 바꾸면 GitHub Pages 사이트가 **완전히 내려간다**
>    (404). `public`으로 되돌려도 자동 복구가 안 되고 Pages를 API로 재생성해야 한다
>    (`gh api repos/.../pages -X POST -f "source[branch]=main" -f "source[path]=/"`).
>    자세한 경위는 2-3절 참고.

---

## 1. 프로젝트가 뭔가

카카오맵 기반 영어 회화 연습 앱. 정적 파일 하나(`index.html`)로 동작.
공개 저장소, GitHub Pages로 배포됨.

- 상단 제목은 한 줄 왼쪽/오른쪽 정렬: 왼쪽 "실전영어회화!-AI 취업역량강화", 오른쪽 "IT융합컴퓨터과 송정호"
  (취업역량강화 사업의 학과 자료로 쓰임 — CLAUDE.md 1번 항목은 이 맥락 기준으로 계속 갱신할 것).
- 지도 위 업종별 마커를 클릭하면 하단 시트에 영어 대화 예문(직원/손님 역할극)이 뜬다(보통 15개 시나리오,
  회사만 25개). 각 문장 밑에 한글 발음 표기(`.pron`)가 있고, 문장 옆 스피커 아이콘을 누르면 브라우저 TTS로
  실제 발음을 들을 수 있다(재생 속도 0.63 — 기본보다 느리게, 발음 학습 목적).
- 16개 업종, 상단 칩 순서: 편의점·카페·음식점·주유소·도서관·병원·약국·대형마트·은행·주차장·숙박·지하철·
  문화시설·학교(SC4)·어린이집(PS3)·**회사(OFC, 맨 끝, 25개 시나리오·50문장으로 제일 많음)**.
  학교/어린이집/일반 카테고리는 Kakao 공식 카테고리 코드. **도서관(LIB)과 회사(OFC)는 Kakao에 해당
  카테고리 코드가 없어서 keywordSearch("도서관"/"회사")로 대체**(정확도가 카테고리 코드보다 떨어질 수 있음 —
  예: "회사"로 검색하면 볼링장 등 상호에 "회사"가 들어간 무관한 곳도 섞여 나올 수 있음, 알려진 한계).
- 툴바에 "💬 번역" 버튼 — 입력한 말을 MyMemory 무료 API(CORS 허용됨, 키 불필요)로 번역하고,
  영어 결과에는 자체 규칙 기반 사전+전사 알고리즘(`PRON_WORD_DICT` + `transliterateWord`)으로
  한글 발음을 추정해 보여준다. 기존 394+문장의 사람이 직접 만든 발음과는 품질 차이가 크다는 걸
  UI에 명시해뒀음(의도된 것, 개선 여지 있음).
  **다문화 학생 지원(2026-08-10 추가)**: "모국어" 선택(한국어/영어/베트남어/중국어/필리핀어(타갈로그)/
  태국어/몽골어/러시아어/캄보디아어/우즈베크어/네팔어/일본어)과 "변환 결과" 선택(영어만/한국어만/
  영어+한국어)을 번역창에 추가. 모국어와 같은 언어로 변환하는 옵션은 자동 비활성화(한국어 선택 시
  "한국어만"·"영어+한국어" 막힘, 영어 선택 시 "영어만"·"영어+한국어" 막힘).

  **번역 엔진(2026-08-10, MyMemory→구글 우선으로 교체)**: 처음엔 MyMemory 무료 API만 썼는데 실사용
  테스트에서 일본어/타갈로그/몽골어 등 여러 언어에서 요청한 언어와 다른 언어가 반환되거나(예: 한국어
  요청인데 "Korean"이라는 영어가 옴, 완전히 다른 뜻으로 오역) 하는 사례가 다수 확인됨. 대안 조사 결과
  Bing은 CORS가 없어 브라우저 직접 호출 불가, LibreTranslate는 API 키 필수로 바뀜, DeepL/Papago/Azure는
  키가 필요해 정적 사이트에 넣으면 노출·도용 위험 — **구글 번역 비공식 엔드포인트**
  (`translate.googleapis.com/translate_a/single?client=gtx`)만 CORS 허용(`*`)에 키도 불필요해서
  채택. 품질이 확연히 좋아짐(테스트한 실패 케이스 전부 해결). 단, 비공식이라 구글이 언제든 막을 수
  있어 **MyMemory를 폴백으로 유지**(`translateText()` 함수가 구글 실패 시 자동 전환). 그래도 결과가
  틀린 언어로 나올 가능성에 대비해 결과에 한글/라틴 문자가 하나도 없으면 "변환 실패" 경고로 대체하는
  방어 로직(`containsHangul`/`containsLatin`)을 유지 중.

  **마이크 입력(2026-08-10 추가)**: 크메르어·네팔어·몽골어 등은 전용 키보드(IME)가 폰에 없으면 애초에
  타이핑 자체가 안 되는 문제가 있어, 브라우저 내장 Web Speech API(`SpeechRecognition`)로 마이크 음성
  입력 버튼 추가. 미지원 브라우저(구형 사파리 등)에서는 자동으로 버튼 비활성화. 언어별 인식 품질은
  검증 못 함(마이크로 실제 사람 목소리가 필요해서 코드로 테스트 불가 — 실사용 테스트 필요).

  **한국어 출력은 영어 경유(2026-08-10 추가)**: 모국어->한국어 직접 번역 대신, 모국어->영어->한국어로
  한 번 더 거치도록 변경(`needEnForPivot`). 실측 비교 결과 이미 잘 되던 언어는 품질 차이 없고,
  일본어 히라가나처럼 문제 있던 케이스는 개선됨(예: "에키는 어디입니까?"(오역) -> "기차는 어디에
  있나요?"(EN 결과 "Where is the train?"과 일치하는 정상 문장)). 모국어가 영어면 그대로 en->ko.

  **아직 결정 안 된 것**: 모국어 목록 12개(한국어·영어 포함)를 실제 학교 다문화 학생 구성에 맞게
  줄일지는 보류 중. 실사용 테스트 후 판단하기로 함(2026-08-10 대화에서 결정).
- 주유소는 유가 정보를 카카오 자체 장소 페이지 링크로 연결(백엔드 서버 없이).
- 도서관은 잔여 좌석 정보를 보여준다. 브라우저가 data.go.kr을 직접 호출하지 않고,
  GitHub Actions(`.github/workflows/update-library-data.yml`, 20분 간격)가 대신 호출해
  `data/lib-index.json` / `data/lib-seats.json`으로 스냅샷을 커밋하면 그 파일을 같은 오리진에서 읽는 구조
  (완전 실시간 아님, 패널에 기준 시각 표시). 전국 168개 도서관만 커버, 매칭 안 되면 "잔여 좌석 정보 없음".
  **워크플로가 조용히 실패할 수 있다 — 2-2절 참고.**
  **좌석 유무 마커 색 구분(2026-08-18 추가)**: 도서관 카테고리를 볼 때만 마커·목록 항목 색이
  카테고리 고유색(보라) 대신 초록(`#16A34A`, 좌석 스냅샷 있음)/회색(`#9CA3AF`, 없음)으로 바뀐다.
  판정 로직은 정보 시트가 여는 시점에 하던 매칭(`findMatchingLibrary`, 400m 이내 최근접)+행 조회
  (`fetchLibrarySeatRows`)를 마커 그리기 전에(`searchNearby`) 미리 돌리는 방식 — 15곳이어도
  실제 네트워크 요청은 index/seats 스냅샷 각각 1회뿐(프로미스 캐시). 사이드바 목록에도 같은 색
  점(`.result-dot`)을 표시. 상단 `.map-hint` 아래 범례(`#libLegend`)는 도서관 카테고리일 때만 보임.
- 내 위치 버튼을 누르면 지도 중심 이동 + 파란 점 마커가 실제 위치로 이동(마커 참조를 들고 있다가 setPosition, 매번 새로 만들지 않음).

## 2. 상태

핵심 기능 전부 구현·배포·검증 완료. 도서관 좌석 자동 갱신 워크플로는 GitHub Actions에 `active`
상태로 등록되어 20분마다 도는 **중이어야 하는데, 2026-08-13~08-18 사이 5일간 조용히 전량 실패**했다가
2026-08-18에 재정상화됨(2-2절). **다음 세션은 지금도 잘 돌고 있는지 반드시 재확인할 것** —
한 번 조용히 죽었던 전적이 있는 만큼 "예전에 고쳤으니 괜찮겠지"라고 넘기지 말 것.

**최종 갱신: 2026-08-18**
- [x] 도서관 워크플로 5일 장애 원인 규명·수정·재검증 완료(2-2절)
- [x] 도서관 마커 좌석 유무 색 구분 추가(초록/회색), 배포 완료
- [ ] 이번 세션 작업분 커밋·푸시 — **문서 갱신 직후 아직 안 함, 다음 확인할 것**

**데스크톱 반응형 버그 2건 수정·배포 완료(2026-08-10):**
- 데스크톱(폭 900px 이상, 사이드바 380px 고정)에서 "주변" 버튼을 눌러도 카테고리 16개 그리드가
  거의 항상 안 보이던 문제. 원인: `.cat-panel`에 `overflow-y:auto`가 걸려 있어 flexbox 스펙상
  자동 최소 높이가 0으로 계산됨 → 사이드바 세로 공간이 부족할 때(흔한 경우) 패널이 실제 높이 1px로
  찌그러짐. `.cat-panel { flex-shrink: 0; }` 추가로 해결.
- 데스크톱 사이드바(380px)에서 왼쪽 제목이 잘리던 문제. 원인은 두 가지가 겹쳐 있었음: (1) 17px
  폰트가 380px 폭에는 애초에 너무 큼 (2) 처음 넣은 min-width:900px 축소 규칙이 CSS 소스 순서상
  기본 `.title-left{font-size:17px}` 선언보다 앞에 있어서 캐스케이드에 덮어써짐. 기본 선언 뒤에
  있는 min-width:900px 블록으로 옮기고 기존 검증된 좁은화면용 값(14px/10.5px)을 재사용해 해결.
- 모바일 뷰(390px 폭)에서 회귀 없음, CDP로 재확인함.

## 2-1. 이번에 겪은 인증 문제 기록 (다음에 비슷한 일 생기면 참고)

`.github/workflows/*.yml` 파일을 포함한 push가 계속 "refusing to allow an OAuth App ...
without workflow scope"로 거부된 적이 있었다. 순서대로 시도한 것과 결과:
1. Git Credential Manager 재로그인 → 스코프 그대로(`gist, read:org, repo`), 해결 안 됨.
2. `workflow` 스코프 포함한 classic PAT(ghp_...) 발급 → `/user` API로 스코프 확인해봐도 정상인데
   여전히 거부됨. `git config --local credential.helper store`로 바꿔도 전역 GCM이 먼저 응답해서
   무시되고 있었음 → `credential.helper ""` 리셋 후 `store`만 남기는 것까지 했는데도 **여전히 거부**.
   GitHub Contents API로 직접 PUT 해봐도 워크플로 경로만 404(다른 경로는 201 성공) — 토큰 스코프
   표시와 실제 워크플로 파일 쓰기 권한이 어긋나는, 원인 불명의 GitHub 쪽 문제였다.
3. **최종 해결책: GitHub 웹 화면에서 직접 파일 생성/커밋.** 브라우저 로그인 세션은 이 OAuth App
   스코프 제약을 안 받는다. 이후 로컬 git 히스토리와 merge(파일 내용이 remote와 동일하면 push가
   막히지 않는다 — 실제로 뭔가 달라져야 막힘).
결론: git push나 API 토큰으로 워크플로 파일이 안 올라가면, 스코프를 아무리 맞춰도 안 풀릴 수 있다.
**바로 GitHub 웹 UI에서 직접 만들라고 안내하는 게 시간을 아낀다.**

> **갱신(2026-08-18)**: 이번엔 반대로 `git push`는 여전히 막혔지만
> **`gh api .../contents/... -X PUT`(Contents API)은 성공했다.** 즉 막히는 경로가 그때그때
> 다르다(원인 불명, GitHub 쪽 문제로 추정 — 위 결론은 여전히 유효). **권장 순서**:
> 1) `git push` 시도 → 막히면 2) `gh api contents PUT` 시도(빠르고 이력이 깔끔하게 남음) →
> 그것도 막히면 3) 웹 UI. **웹 UI로 고칠 땐 들여쓰기 각별히 주의** — 2-2절에서 실제로 이 실수로
> YAML이 깨져 5일간 조용히 장애가 났다.

## 2-2. 도서관 워크플로 5일 장애 사고 기록 (2026-08-13 ~ 08-18)

**증상**: 사이트는 멀쩡히 떴지만(정적 페이지라 장애가 안 보임), 도서관 잔여좌석 데이터가
8/13 이후로 갱신이 멈춰 있었다. 사용자가 "잔여좌석이 나와야 프로그램다우니까"라며 알아채고
확인을 요청해서 발견됨 — **자동 감지 수단이 없었다는 뜻이기도 하다** (개선 여지, 미착수).

**근본 원인**: 8/13 커밋(`10c6403`, API 키를 하드코딩에서 `.env`/GitHub secret으로 옮기는 작업)
이후, 워크플로 시크릿(`LIB_API_KEY`)은 등록됐지만 **워크플로 파일에 그 시크릿을 스텝에 실제로
넘겨주는 `env:` 설정이 빠져 있었다.** 그래서 스크립트가 매번 "LIB_API_KEY가 없습니다"로 즉시
실패. 이걸 고치려던 시도(`env:` 추가)가 2-1절의 workflow-scope 문제로 push가 막혀서
**`a2420e4` 커밋으로 되돌려진 채 5일간 방치**돼 있었다(커밋 메시지 자체에 원인이 적혀있었음:
"Revert workflow env-var change (push blocked without workflow OAuth scope)").

**2차 사고 — 웹 UI 수정 중 YAML 깨짐**: 사용자가 GitHub 웹 UI에서 직접 `env:` 블록을 추가했는데,
`run:`과 `env:`의 들여쓰기 칸 수가 한 칸 어긋나서(`env:`가 `run:`보다 1칸 더 들어감) **YAML
파싱 자체가 실패**하는 새 문제가 생겼다(`mapping values are not allowed here`). Actions 로그가
아니라 **로컬에서 `pyyaml`로 직접 파싱해봐야 확실히 잡히는 종류의 실수**였다 — 육안으로는
안 보였다. 다음에도 워크플로 파일을 고친 뒤에는 반드시 YAML 파서로 검증할 것:
```
pip install pyyaml
python -c "import yaml; yaml.safe_load(open('.github/workflows/파일명.yml', encoding='utf-8'))"
```

**최종 해결**: Contents API PUT으로 올바른 들여쓰기 버전을 다시 올림(위 갱신 메모 참고, 이번엔
API가 통했다) → YAML 파서로 재검증 → `gh workflow run`으로 수동 실행 2회(1회차는
`apis.data.go.kr` 접속이 일시 타임아웃, 2회차 성공) → `data/lib-seats.json`의
`generatedAt`이 방금 시각으로 갱신된 것까지 확인.

**교훈**:
- 워크플로 파일을 고칠 때마다 **로컬 YAML 파서로 검증부터 하고 나서 커밋할 것** — 웹 UI건
  API건 사람이 들여쓰기를 손으로 만지면 틀리기 쉽다.
- 이 워크플로는 실패해도 **사이트가 안 죽어서 아무도 못 알아챈다.** 데이터 무결성 자동 점검
  (songstock의 `logic/integrity_check.py` 같은 것)을 만들 가치가 있다 — 아직 미착수.
- `apis.data.go.kr` 연결이 가끔 타임아웃 난다(1/2 성공) — 일회성 실패로 판단하지 말고 재시도로
  구분할 것. 워크플로 자체에 재시도 로직을 넣는 것도 고려해볼 만하다(미착수).

## 2-3. GitHub Pages는 저장소를 비공개로 바꾸면 죽는다 (2026-08-18 실제 장애)

사용자가 "코드는 필요할 때만 공개하자"는 원칙으로 이 저장소를 포함해 여러 저장소를
비공개로 일괄 전환하려 했다. `gh repo edit --visibility private`로 바꾸자
**GitHub Pages 사이트가 즉시 404로 죽었다.** (참고로 사전에 GitHub 공식 문서를 찾아보고
"Free/Pro 요금제는 저장소를 비공개로 해도 Pages 사이트는 계속 공개로 뜬다"는 문서 내용을
믿고 진행했는데, **실제로는 틀렸다** — 적어도 이 계정/시점 기준으로는 즉시 내려갔다.
문서와 실제 동작이 다를 수 있다는 사례로 기록해둔다.)

- **legacy 빌드**(이 저장소가 씀, `main` 브랜치 루트를 그대로 서빙)는 private 전환 시
  **즉시** 404, `public`으로 되돌려도 Pages 설정 자체가 사라져서(`GET .../pages`가 404)
  수동으로 다시 만들어야 복구된다.
- **workflow 빌드**(healthyplan이 씀, GitHub Actions로 빌드해 `actions/deploy-pages`로
  배포)는 private 전환 직후엔 잠깐 살아있는 것처럼 보였지만(200) **몇 분 뒤 결국 같이
  내려갔다**(지연된 반영으로 추정). 복구하려면 Pages를 재생성(`build_type=workflow`로
  POST)한 다음 배포 워크플로를 수동 재실행(`gh workflow run "Deploy to GitHub Pages"`)해야
  한다 — 단순 재실행만으로는 안 됨(`actions/deploy-pages`가 "Pages가 활성화돼 있어야
  배포 가능"이라며 404로 거부하기 때문에 Pages 재생성이 먼저다).
- 두 경우 다 실제로 겪고 복구까지 완료함(다운타임 각각 수 분). 사용자가 바로 "승인" 요청에
  응답해줘서 빨리 복구됐다 — **다음에 누군가 비공개 전환을 다시 시도하면 곧바로 같은 장애가
  재현된다는 뜻이니, 시도 자체를 하지 말 것.**

**결론 — 이 계정의 실제 정책**: songstock·scenemap-app·biz-eng-map-contest처럼 **사람이 직접
쓰는 개인용/미공개 도구는 비공개로 유지.** biz-eng-map·healthyplan처럼 **GitHub Pages로
띄워서 남(학생 등)이 URL로 접속해 쓰는 서비스는 공개로 유지**할 수밖에 없다(Enterprise
요금제가 아닌 한 대안이 없음). "코드는 필요할 때 공개" 원칙은 코드 전용 저장소에는
적용하되, 이미 배포된 서비스 저장소 두 곳은 예외로 둔다.

**2026-08-18 사용자 재확인 + 전체 점검**: "외부서비스 안 하는 프로그램들은 깃에서
비공개로 유지되어야 한다"고 명시적으로 재확인받고, `gh repo view`로 전체 점검함 —
결과는 `C:\ai_project\_백업현황.md`의 "GitHub 공개/비공개 정책 점검" 절 참고. 이
CLAUDE.md는 장애 경위·정책 근거용, 실제 점검 결과 체크리스트는 그쪽에 둠(여러
저장소를 한곳에서 봐야 하는 성격이라).

## 3. 알아둘 것

- **카카오맵 JS 키는 `localhost:3000`뿐 아니라 GitHub Pages 배포 도메인(`powercm77-git.github.io`)에도
  등록되어 있다.** 실제 배포 사이트에서는 정상 동작 확인됨(CDP로 마커 클릭·정보 시트·대화 탭까지 테스트 완료).
  로컬에서 띄워 테스트할 때는 여전히 3000번 포트로 서빙해야 한다(`npx serve -l 3000` 등). 다른 포트면
  지도 자체가 안 뜨는 것처럼 보이니 착각하지 말 것(서버 문제가 아니라 도메인 락 문제).
- 로컬 3000번 포트는 다른 프로젝트(trip-planner) 서버가 이미 점유하고 있을 수 있다.
  그 서버를 임의로 종료하지 말고, 로컬 테스트가 필요하면 먼저 확인할 것.
- 도서관 API 서비스키는 더 이상 `index.html`에 없음 — `scripts/fetch-library-data.js`(서버 측,
  GitHub Actions 전용)로 옮겼다. 브라우저는 API를 직접 호출하지 않는다.
- 폰(LAN)으로 접속 테스트할 때는 이 앱 자체는 정적 파일이라 문제없지만, 카카오맵 도메인 락 때문에
  `localhost:3000`으로 접속해서 테스트하는 게 기본이다(LAN IP로는 지도가 안 뜬다는 점 주의).
- **배포는 GitHub Pages뿐이다(Netlify 아님)** — `main` 브랜치 루트를 그대로 서빙, 빌드 스텝 없음.
  라이브 주소: https://powercm77-git.github.io/biz-eng-map/ . 확인 명령:
  `gh api repos/powercm77-git/biz-eng-map/pages` (status가 `built`인지),
  `gh api repos/powercm77-git/biz-eng-map/pages/builds/latest` (commit이 최신 로컬 HEAD와 일치하는지).
